<?xml version="1.0" encoding="UTF-8"?>
<vxml version="2.0" xml:lang="fr-FR">
  <form id="accueil">
    <block>
      <prompt bargein="true">
        Bienvenue. Tapez : 1 pour sport, 2 pour loisir, 3 pour sortie, 4 pour santé, 5 pour actualité.
        Appuyez sur dièse pour répéter.
      </prompt>
      <goto next="#menu_principal"/>
    </block>
  </form>

  <menu id="menu_principal" dtmf="true">
    <choice dtmf="1" next="#menu_sport"><prompt>1 pour sport</prompt></choice>
    <choice dtmf="2" next="#loisir"><prompt>2 pour loisir</prompt></choice>
    <choice dtmf="3" next="#sortie"><prompt>3 pour sortie</prompt></choice>
    <choice dtmf="4" next="#sante"><prompt>4 pour santé</prompt></choice>
    <choice dtmf="5" next="#actu"><prompt>5 pour actualité</prompt></choice>
    <noinput><prompt>Je n'ai rien entendu. Répétez votre choix.</prompt><reprompt/></noinput>
    <nomatch><prompt>Choix invalide. Répétez votre choix.</prompt><reprompt/></nomatch>
  </menu>

  <!-- Sous-menu SPORT -->
  <form id="menu_sport_intro">
    <block>
      <prompt>Rubrique sport. Tapez 1 pour football, 2 pour tennis, 9 pour retour.</prompt>
      <goto next="#menu_sport"/>
    </block>
  </form>

  <menu id="menu_sport" dtmf="true">
    <choice dtmf="1" next="#football"><prompt>1 football</prompt></choice>
    <choice dtmf="2" next="#tennis"><prompt>2 tennis</prompt></choice>
    <choice dtmf="9" next="#menu_principal"><prompt>9 retour</prompt></choice>
    <noinput><prompt>Pas d'entrée. Répétez.</prompt><reprompt/></noinput>
    <nomatch><prompt>Invalide. Répétez.</prompt><reprompt/></nomatch>
  </menu>

  <form id="football">
    <block>
      <prompt>Dernières infos football. (À remplacer.)</prompt>
      <goto next="#menu_sport_intro"/>
    </block>
  </form>

  <form id="tennis">
    <block>
      <prompt>Actu tennis. (À remplacer.)</prompt>
      <goto next="#menu_sport_intro"/>
    </block>
  </form>

  <!-- Squelettes autres rubriques -->
  <form id="loisir"><block><prompt>Rubrique loisir.</prompt><goto next="#menu_principal"/></block></form>
  <form id="sortie"><block><prompt>Rubrique sortie.</prompt><goto next="#menu_principal"/></block></form>
  <form id="sante"><block><prompt>Rubrique santé.</prompt><goto next="#menu_principal"/></block></form>
  <form id="actu"><block><prompt>Rubrique actualité.</prompt><goto next="#menu_principal"/></block></form>

  <form id="fin"><block><prompt>Merci. Au revoir.</prompt><disconnect/></block></form>
</vxml>
